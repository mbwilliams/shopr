<%= form_for @store do |f| %>
  <% if @store.errors.any? %>
  <div id="error_explanation" class="alert error">
    <strong><%= pluralize(@store.errors.count, "error") %> prohibited
      this store from being saved:</strong>
    <ul>
    <% @store.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
