<% if resource.errors.any? %>
<div id="error_explanation" class="alert error">
  <strong><%= pluralize(resource.errors.count, "error") %> prohibited
    this store from being saved:</strong>
  <ul>
  <% resource.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
